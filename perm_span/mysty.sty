\ProvidesPackage{mysty}

\newcommand\thetitle{On the span of the permutation matrices}
\newcommand\theauthor{Izaak van Dongen (imv26)}

\RequirePackage[margin=2cm]{geometry}

\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}

\RequirePackage{tcolorbox}
\RequirePackage{adjustbox}
\RequirePackage{parskip}

% \RequirePackage{enumerate}
\RequirePackage{enumitem}

\RequirePackage{multicol}

\RequirePackage[hidelinks,pdfencoding=auto,psdextra,
 pdfauthor={\theauthor},
 pdftitle={\thetitle},
 pdfsubject={\thetitle},
 pdfproducer={LaTeX with hyperref},
 pdfcreator={pdflatex}]{hyperref}

% prevent unauthorised line breaks in inline equations
\binoppenalty=\maxdimen
\relpenalty=\maxdimen
